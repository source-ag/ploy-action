name: 'Ploy Action'
description: 'GitHub Action for Ploy, a simple deployment tool'
author: 'DonDebonair'
branding:
  color: 'green'
  icon: 'truck'

inputs:
  version:
    description: 'Ploy version'
    default: 'latest'
    required: false
  deployment-file:
    description: 'Ploy configuration file with services to deploy'
    required: true
  verify-only:
    description: 'Just verify deployments'
    default: 'false'
    required: false

runs:
  using: 'node16'
  main: 'dist/index.js'
